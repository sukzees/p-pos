rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to authenticated users
    // For production, you should make this more restrictive
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
